class Player {
  constructor(name) {
    this.name = name;
    this.wins = 0;
    this.choice = '';
  }

  chooseFighter(fighter) {
    this.choice = fighter;
  //pass in whicheverone it chose, then this.choice to whatever is passed in.
}

  takeTurns(){

  }
}
